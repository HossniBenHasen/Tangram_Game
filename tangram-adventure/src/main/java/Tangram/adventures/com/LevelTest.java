package Tangram.adventures.com;

import org.junit.Test;

import static org.junit.Assert.*;

public class LevelTest {



    @Test
    public void WHEN_totalPerimeter_retrurnTotalPermimeter(){
/*        Rectangle michel = new Rectangle(5,10,2,6);
        Square patrick = new Square(8,2,2);
        Level levelTest = new Level();
        levelTest.addForm(michel);
        levelTest.addForm(patrick);
        assertEquals(michel.CalculatePerimeter() + patrick.CalculatePerimeter(), levelTest.getTotalPerimeter(), 0.0);*/
    }

    @Test
    public void WHEN_totalSurface_returnTotal(){
       /* Rectangle michel = new Rectangle(5,10,7,8);
        Square patrick = new Square(8,3,3);
        Level levelTest = new Level();
        levelTest.addForm(michel);
        levelTest.addForm(patrick);
        assertEquals(michel.CalculateSurface() + patrick.CalculateSurface(), levelTest.getTotalSurface(), 0.0);*/
    }

}